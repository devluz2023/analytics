apiVersion: v1
kind: Service
metadata:
  name: hello-kubernetes
  namespace: app-backend
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
  selector:
    name: hello-kubernetes